# Copia a .env si quieres personalizar (opcional; los valores por defecto ya apuntan a docker-compose)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=urbanbike
DB_USER=postgres
DB_PASSWORD=postgres

# Solo en Render (perfil prod)
# SPRING_PROFILES_ACTIVE=prod
# API_KEY=tu-clave-secreta
